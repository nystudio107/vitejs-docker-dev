{
  "name": "vitejs-docker-dev",
  "description": "Local development environment for developing vite.js via Docker container",
  "version": "1.0.0",
  "keywords": ["vite", "vitejs", "local", "development", "development-environment","docker", "docker-container"],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "@types/jest": "^27.4.0"
  }
}
